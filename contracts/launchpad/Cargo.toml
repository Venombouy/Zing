[package]
name = "zing-launchpad"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
soroban-sdk = "20.4.0"

[dev-dependencies]
soroban-sdk = { version = "20.4.0", features = ["testutils"] }
